# 语音日历工具 — 需求说明书（PRD）

> **文档版本**：v1.0
> **撰写人**：设计原型专家
> **日期**：2026-05-29
> **项目代号**：Voice Calendar

---

## 一、文档信息

| 条目 | 内容 |
|------|------|
| 项目名称 | 语音日历工具（Voice Calendar） |
| 文档类型 | 需求说明书（PRD） |
| 版本 | v1.0 |
| 撰写人 | 许明需（需求发现分析师） |
| 审核人 | 待定 |
| 日期 | 2026-05-29 |
| 状态 | 已完成 |

---

## 二、项目背景

### 2.1 问题陈述

传统日历工具（如苹果日历、Google Calendar）虽然功能完善，但**事件添加流程繁琐**：
- 用户需要：打开应用 → 点击"+" → 填写标题、时间、地点、备注 → 保存
- 在多任务场景或**手不方便的场景**下，这种操作方式效率低下

### 2.2 机会点

**DeepSeek API 的成熟**（语音识别 ASR + 大语言模型 LLM）为日历交互提供了全新的可能性：

> **用自然语言语音输入，自动解析并创建日历事件**

### 2.3 项目目标

**核心目标**：打造一款**语音优先（Voice-First）**的网页端日历工具，让用户通过语音即可完成日历管理的主要操作，**提升效率 10 倍以上**。

**成功指标**（建议后续确认）：
- 语音添加事件的成功率 > 95%
- 用户完成"添加事件"操作的时间 < 10 秒
- 用户对语音交互的满意度 > 4.5/5

---

## 三、目标用户与核心痛点

### 3.1 目标用户画像

| 维度 | 描述 |
|------|------|
| **核心用户** | 商务人士 |
| **年龄** | 25-45 岁 |
| **职业特征** | 会议密集、日程多变、经常出差 |
| **技术素养** | 中高，熟悉 Siri/小爱同学等语音助手 |
| **使用设备** | 主要在电脑网页端，偶尔用手机浏览器 |
| **使用场景** | 办公室、家里、通勤途中（手不方便时） |

### 3.2 核心痛点（按优先级排序）

#### P0（必须有）
1. **添加事件太慢**
   - 传统方式需要多次点击和输入
   - 商务人士经常需要快速记录临时会议/电话

2. **语音助手不够智能**
   - Siri/小爱同学对中文自然语言理解能力差
   - 无法理解复杂表达（如"明天下午跟张三讨论Q3预算"）

#### P1（应该有）
3. **经常忘记查看日历**
   - 需要主动打开应用才能看到日程
   - 希望有主动提醒功能

4. **修改/删除事件流程繁琐**
   - 需要找到事件 → 点击编辑 → 修改 → 保存
   - 希望能语音直接说"把明天的会议改到后天"

#### P2（最好有）
5. **查找空闲时间困难**
   - 需要手动翻看日历才能回答"下个月有没有空闲时间"
   - 希望能自然语言查询

---

## 四、核心功能清单

### 4.1 P0 — 必须有（MVP 核心功能）

| 功能 | 描述 | 语音交互示例 |
|------|------|--------------|
| **语音添加事件** | 说一句话自动解析并创建事件 | "明天下午3点跟张三讨论Q3预算" |
| **语音删除事件** | 语音指定事件并删除 | "删除明天下午的会议" |
| **语音查看事件** | 语音查询某时段的日程 | "我今天有什么安排" |
| **确认环节** | 执行操作前语音播报确认 | "已为你添加：明天15:00与张三讨论Q3预算，是否确认？" |
| **日历三视图** | 日视图/周视图/月视图切换（参考苹果日历） | - |
| **事件展示** | 在日历上直观展示事件（时间、标题、颜色区分） | - |

### 4.2 P1 — 应该有（提升体验）

| 功能 | 描述 | 语音交互示例 |
|------|------|--------------|
| **语音编辑事件** | 修改事件的时间/标题/参与者 | "把明天上午的会议改到下午3点" |
| **语音搜索事件** | 自然语言搜索日程 | "我下周有什么安排" |
| **智能提醒** | 基于时间或位置的主动提醒 | "距离会议开始还有30分钟" |
| **事件详情查看** | 点击事件查看详细信息（标题、时间、地点、备注） | - |
| **拖拽调整时间** | 在日历上直接拖拽事件调整时间 | - |

### 4.3 P2 — 最好有（未来版本）

| 功能 | 描述 | 语音交互示例 |
|------|------|--------------|
| **自然语言查询** | 复杂查询（空闲时间、冲突检测） | "下个月有没有空闲时间" |
| **智能建议** | AI 分析日程习惯并给出建议 | "你这周会议时间占比过高，建议休息" |

---

## 五、语音交互设计方案

### 5.1 核心交互流程

```
┌───────────────┐
│  用户点击语音按钮                                 │
└───────────────┬─────────────────────────────────┘
                  │
                  ▼
┌───────────────┐
│  录音中...（显示动画波形）                        │
│  - 支持打断：用户可随时停止录音                  │
│  - 超时自动结束：5秒无声音自动停止               │
└───────────────┬─────────────────────────────────┘
                  │
                  ▼
┌───────────────┐
│  DeepSeek ASR 语音识别                           │
│  - 输入：音频                                    │
│  - 输出：识别文本（如"明天下午3点开会"）         │
└───────────────┬─────────────────────────────────┘
                  │
                  ▼
┌───────────────┐
│  DeepSeek LLM 自然语言理解                       │
│  - 输入：识别文本 + 当前日历上下文              │
│  - 输出：结构化事件数据                          │
│    {                                                 │
│      "intent": "add_event",                         │
│      "title": "开会",                               │
│      "start_time": "2026-05-30 15:00",             │
│      "end_time": "2026-05-30 16:00",               │
│      "participants": ["张三"],                        │
│      "location": "",                                 │
│      "confidence": 0.95                             │
│    }                                                 │
└───────────────┬─────────────────────────────────┘
                  │
                  ▼
┌───────────────┐
│  置信度判断                                      │
│  - confidence >= 0.9 → 直接执行（可选）           │
│  - 0.7 <= confidence < 0.9 → 需要确认          │
│  - confidence < 0.7 → 请求用户重新说             │
└───────────────┬─────────────────────────────────┘
                  │
                  ▼
┌───────────────┐
│  确认环节（默认开启，可在设置中关闭）             │
│  - 语音播报："已为你添加：明天15:00开会，是否确认？" │
│  - 用户回答："确认" / "取消" / "修改"            │
└───────────────┬─────────────────────────────────┘
                  │
                  ▼
┌───────────────┐
│  执行操作                                          │
│  - 添加/删除/修改事件                             │
│  - 更新日历视图                                    │
│  - 语音反馈："已添加" / "已删除"                  │
└───────────────┘
```

### 5.2 语音交互设计要点

#### 5.2.1 唤醒方式
- **方案 A（推荐）**：点击开始语音输入

#### 5.2.2 容错机制
- **识别失败**："抱歉，我没听清，请再说一遍"
- **解析失败**："无法理解你的意思，请尝试说'明天下午3点开会'"
- **歧义处理**："你是指今天下午3点，还是明天下午3点？"

#### 5.2.3 多轮对话支持
- **场景**：用户说"明天下午3点开会"，系统问"会议标题是什么？"
- **实现**：维护对话上下文，支持追问

#### 5.2.4 语音反馈设计
- **成功反馈**：简短确认（"已添加" / "已删除"）
- **失败反馈**：说明原因并给出建议
- **确认反馈**：重复关键信息（"已为你添加：明天15:00开会"）

---

## 六、界面设计方向

### 6.1 设计参考：苹果日历（Apple Calendar）

✅ 左侧月视图日历 - 显示完整月份，可点击导航
✅ 右侧事件展示区 - 显示选中日期的事件
✅ 日/月/年视图切换 - 三种视图模式切换正常
✅ 语音控制面板 - 带有状态提示和使用指南
✅ 事件的颜色标识 - 不同事件用不同颜色区分
- **左侧**：迷你月视图日历，可导航月份、快速跳转"今天"、点击选择日期
- **右侧**：事件面板，支持日/月/年三种视图切换，显示对应时间范围的事件
- **语音控制**：位于左下方，支持语音添加、删除、查询事件（需在支持 Web Speech API 的浏览器中使用，如 Chrome）
- **语音指令示例**：

- "添加明天下午3点开会"
- "删除团队周会"
- "查看今天的安排"

语音识别会显示实时状态反馈（聆听中、处理中、成功、失败），并在识别完成后自动更新日历和事件列表。

---

## 七、技术实现要点

### 7.1 DeepSeek API 集成方案

#### 7.1.1 语音识别（ASR）
- **API**：DeepSeek ASR API
- **输入**：WebM/MP3 音频格式
- **输出**：识别文本（中文）
- **前端实现**：
  ```javascript
  // 使用 Web Speech API 或 RecordRTC 录音
  const mediaRecorder = new MediaRecorder(stream);
  mediaRecorder.ondataavailable = (event) => {
    audioChunks.push(event.data);
  };
  mediaRecorder.onstop = async () => {
    const audioBlob = new Blob(audioChunks, { type: 'audio/webm' });
    const text = await callDeepSeekASR(audioBlob);
  };
  ```

#### 7.1.2 自然语言理解（NLU）
- **API**：DeepSeek Chat API（使用 function calling）
- **Prompt 设计**：
  ```
  你是一个日历助手，需要从用户语音中提取以下信息：
  - intent: add_event / delete_event / view_events / edit_event
  - title: 事件标题
  - start_time: 开始时间（ISO 8601格式）
  - end_time: 结束时间（ISO 8601格式）
  - participants: 参与者列表
  - location: 地点

  当前时间：2026-05-29 14:30
  用户输入：{user_input}

  请以JSON格式输出。
  ```
- **Function Calling 定义**：
  ```json
  {
    "name": "extract_calendar_event",
    "parameters": {
      "type": "object",
      "properties": {
        "intent": { "type": "string", "enum": ["add_event", "delete_event", "view_events", "edit_event"] },
        "title": { "type": "string" },
        "start_time": { "type": "string", "format": "date-time" },
        "end_time": { "type": "string", "format": "date-time" },
        "participants": { "type": "array", "items": { "type": "string" } },
        "location": { "type": "string" }
      }
    }
  }
  ```

### 7.2 前端技术栈建议

| 技术 | 选型 | 理由 |
|------|------|------|
| **框架** | React / Vue | 组件化开发，生态成熟 |
| **日历组件** | FullCalendar / 自研 | FullCalendar功能全，但定制成本高 |
| **状态管理** | Zustand / Pinia | 轻量级，适合中小项目 |
| **样式方案** | Tailwind CSS | 快速开发，易于定制 |
| **语音录制** | RecordRTC | 跨浏览器支持好 |
| **API 请求** | Axios / Fetch | - |

### 7.3 数据持久化方案

#### 方案 A（推荐）：云端同步
- **优点**：多设备同步，数据不丢失
- **缺点**：需要用户登录，服务器成本
- **实现**：后端 API + 数据库（PostgreSQL）

#### 方案 B：本地存储
- **优点**：无需登录，隐私保护好
- **缺点**：无法多设备同步，数据易丢失
- **实现**：LocalStorage / IndexedDB

**建议**：MVP 阶段先实现本地存储，后续版本加入云端同步。

---

## 八、开放问题（需要用户确认）

### 8.1 高优先级待确认

1. **是否需要用户登录系统？**
   - 如果不需要登录 → 使用本地存储
   - 如果需要登录 → 需要设计注册/登录流程

2. **语音识别用哪个 API？**
   - DeepSeek ASR（需要确认是否支持实时流式识别）
   - 或者其他 ASR 服务（讯飞、阿里云、腾讯云）

3. **确认环节是否可关闭？**
   - 建议：设置页提供"跳过确认"开关（高级用户可选）

4. **事件提醒如何实现？**
   - 浏览器通知（Notification API）
   - 邮件提醒（需要用户绑定邮箱）
   - 微信/企业微信提醒（需要集成）

### 8.2 中优先级待确认

5. **是否需要支持多日历？**
   - 例如：工作日历、个人日历、节假日日历

6. **事件是否支持重复？**
   - 例如：每周一上午10点开会

7. **语音交互是否支持离线？**
   - 离线 ASR 准确率低，建议强制联网

### 8.3 低优先级待确认

8. **是否需要导出日历？**
   - 导出为 iCal 格式（.ics）

9. **是否需要与他人共享日历？**
   - 例如：团队日历、家庭日历

10. **是否需要深色模式？**
    - 参考苹果日历，暂时可以不做

---

## 九、项目里程碑（建议）

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| **需求确认** | Week 1 | 需求说明书（本文档） |
| **设计阶段** | Week 2-3 | 原型文件、设计系统规范 |
| **开发 MVP** | Week 4-6 | 核心功能（语音添加/删除/查看事件） |
| **测试与迭代** | Week 7-8 | 用户测试反馈、功能优化 |
| **发布 v1.0** | Week 9 | 正式版本发布 |

---

## 十、附录

### 10.1 竞品分析（简要）

| 产品 | 优点 | 缺点 |
|------|------|------|
| **苹果日历** | 设计优美，交互流畅 | 不支持语音添加 |
| **Google Calendar** | 功能强大，与 Google 生态集成 | 语音识别需配合 Google Assistant |
| **Fantastical** | 支持自然语言输入（文字） | 不支持语音，价格高 |
| **Any.do** | 支持语音添加任务 | 日历功能弱 |

**本产品机会点**：语音 + 自然语言理解 + 苹果日历级设计

### 10.2 参考资料

- [Apple Calendar Design Guidelines](https://developer.apple.com/design/human-interface-guidelines/calendar)
- [DeepSeek API 文档](https://platform.deepseek.com/docs)
- [Web Speech API - MDN](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)

---

**需求说明书已完成，可进入设计阶段。** ✅

---
